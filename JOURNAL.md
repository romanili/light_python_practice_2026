# Журнал работ

## 2026-06-29 — Этап 1. Каркас

- **Сделано:** создана структура `src/` с точкой входа `main.py`; программа
  принимает путь к папке аргументом и проверяет его; реализован модуль `db.py`
  с инициализацией SQLite и минимальной схемой таблицы `files`; добавлены
  README, `.gitignore` и `student_meta.md`.
- **Проблемы:** нет.
- **Следующий шаг:** Этап 2 — рекурсивный обход папки и сохранение индекса в SQLite.
